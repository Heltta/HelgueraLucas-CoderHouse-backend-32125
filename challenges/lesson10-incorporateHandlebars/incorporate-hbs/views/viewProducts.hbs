{{> pageTitle pageName="Vista de productos"}}
{{#if listExists}}
    <ul>
        {{#each items}}
            <li>{{this.title}}</li>
        {{/each}}
    </ul>
{{/if}}
{{> redirectBtn link="/" name="Ingresar productos"}}
